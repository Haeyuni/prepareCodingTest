#include <iostream>
using namespace std;

int main() {
    // Please write your code here.
    int a(3), b(5), temp(0);
    temp = 3;
    a = b;
    b = temp;

    cout << a << endl << b;

    return 0;
}