#include <iostream>
using namespace std;

int main() {
    // Please write your code here.
    int year(0), day(0), month(0);

    cin >> day;
    cin.get();
    cin >> month;
    cin.get();
    cin >> year;

    cout << year << "." << day << "." << month;
    return 0;
}