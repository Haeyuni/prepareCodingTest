#include <iostream>
using namespace std;

int main() {
    // Please write your code here.
     int hour(0), minute(0);

     cin >> hour;
     cin.get();
     cin >> minute;
     hour++;
     cout << hour << ":" << minute;
    return 0;
}