#include <iostream>
using namespace std;

int main() {
    // Please write your code here.
    int a(0), b(0), c(0);
    cin >> a >> b >> c;
    int sum = a + b + c;
    cout << sum << endl << sum / 3;
    return 0;
}